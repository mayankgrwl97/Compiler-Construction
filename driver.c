#include <stdlib.h>
#include <stdio.h>
#include "symboltable.h"
#include "hashtable.h"

int main()
{
	// symboltable* table = makesymboltable();
	// addKeywords(table);
	// while(1)
	// {
	// 	tokeninfo* temp = getNextToken(cleanFile, table);
	// }
	return 0;
}